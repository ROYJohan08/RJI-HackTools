# Tips

Add dommain to ip : 
```
sudo tee --append /etc/hosts <<< "{ip} {host}"
```
Path of user list : 
```
/etc/passwd
```
Command for user allowed sudo : 
```
sudo -l
```
Command for read databases : 
```
sqlite3 user.db
```
